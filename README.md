# drift-browser-shell
DRIFT is a fully functional browser shell, a web application that replicates the core experience of a desktop browser. Users can open multiple tabs, navigate the web, bookmark pages, search their history, and have everything sync across devices when signed in.
